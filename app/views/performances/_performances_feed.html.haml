%section.perf-feed.row
  - if performances.length > 0
    - performances.each do |performance|
      .column.small-12
        = render '/performances/feed_item', performance: performance
  - else
    .row
      .column.small-12
        .panel
          %p Stay tuned for more information soon!
