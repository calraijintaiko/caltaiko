:ruby
  title = 'Photo Galleries | Cal Raijin Taiko'
  description = "Images from some of Cal Raijin Taiko's past performances."
  image = URI.join(root_url, image_path('logo.png'))

  meta title: title, description: description,
       og: { title: title, description: description, image: image,
             url: request.original_url }

- if @performances.length > 0
  %header.row
    %h1.small-12.column PHOTO GALLERIES

  %section.galleries-feed
    - @performances_by_year.keys.each do |year|
      .row
        %h4.small-12.column= year
      = render 'pages/galleries_feed', performances: @performances_by_year[year]
- else
  .row
    .column.small-12
      .panel
        %p There are currently no photo galleries; stay tuned!
